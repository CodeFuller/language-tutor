<UserControl x:Class="VocabularyCoach.Views.StudyVocabularyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VocabularyCoach.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="32">спасибо</TextBlock>
        </Grid>

        <Grid Grid.Row="1" Margin="20" VerticalAlignment="Center">
            <TextBox FontSize="32" HorizontalContentAlignment="Center">dziękuję</TextBox>
        </Grid>

        <UniformGrid Grid.Row="2" Margin="20" Rows="1" Columns="2" HorizontalAlignment="Center">
            <Button Margin="0,0,20,0" Content="Check" />
            <Button Margin="20,0,0,0" Content="Finish" Command="{Binding FinishStudyCommand}" />
        </UniformGrid>
    </Grid>
</UserControl>
