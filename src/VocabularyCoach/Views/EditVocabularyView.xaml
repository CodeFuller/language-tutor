<UserControl x:Class="VocabularyCoach.Views.EditVocabularyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:VocabularyCoach.ViewModels.Interfaces"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance interfaces:IEditVocabularyViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="5">
            <ListBox BorderThickness="1" BorderBrush="{DynamicResource PrimaryHueLightBrush}"
                     ItemsSource="{Binding TextsInStudiedLanguage}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <Grid Grid.Column="1" Margin="5,0,5,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Padding="0,5,5,0" Margin="0,10,0,0" Content="{Binding StudiedLanguage.Name}" ContentStringFormat="Text in {0} language:" />
            <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding TextInStudiedLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Row="1" Grid.Column="1" Margin="5,0,5,0" Height="Auto" Padding="0" Style="{DynamicResource MaterialDesignToolButton}" ToolTip="Check"
                    IsEnabled="{Binding TextInStudiedLanguageIsFilled}" Command="{Binding CheckTextCommand}">
                <materialDesign:PackIcon Width="Auto" Height="Auto" Kind="CheckCircle" Foreground="Green" />
            </Button>
            <Label Grid.Row="2" Grid.Column="0" Padding="0,5,5,0" Margin="0,10,0,0" Content="{Binding KnownLanguage.Name}" ContentStringFormat="Text in {0} language:" />
            <TextBox Grid.Row="3" Grid.Column="0" Text="{Binding TextInKnownLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Label Grid.Row="4" Grid.Column="0" Padding="0,5,5,0" Margin="0,10,0,0">Note in Russian language (optional):</Label>
            <TextBox Grid.Row="5" Grid.Column="0" Text="{Binding NoteInKnownLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Label Grid.Row="6" Grid.Column="0" Padding="0,5,5,0" Margin="0,10,0,0">Pronunciation record URL (optional):</Label>
            <TextBox Grid.Row="7" Grid.Column="0"  Text="{Binding PronunciationRecordUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Row="7" Grid.Column="1" Margin="5,0,5,0" Height="Auto" Padding="0" Style="{DynamicResource MaterialDesignToolButton}" ToolTip="Listen"
                    IsEnabled="{Binding PronunciationRecordUrlIsFilled}" Command="{Binding PlayPronunciationRecordCommand}">
                <materialDesign:PackIcon Width="Auto" Height="Auto" Kind="VolumeHigh" />
            </Button>
            <UniformGrid Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" Margin="20" Rows="1" Columns="3" HorizontalAlignment="Center">
                <Button Margin="0,0,20,0" Width="120" Content="Save" Command="{Binding SaveChangesCommand}"/>
                <Button Margin="0,0,20,0" Width="120" Content="Clear" Command="{Binding ClearChangesCommand}"/>
                <Button Margin="0,0,20,0" Width="120" Content="Go to Start" Command="{Binding GoToStartPageCommand}"/>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>
